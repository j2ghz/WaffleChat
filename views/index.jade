extends layout

block content
    div(id="mainContainer")
        h1 WaffleThread
        h2 #{username}
        button(id="createThread") Create a thread
        ul(id="threads")
        a(href='/logout') Logout
    div(id="chatContainer")
